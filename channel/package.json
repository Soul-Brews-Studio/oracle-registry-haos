{
  "name": "oracle-channel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "start": "bun install --no-summary && bun --env-file=/dev/null run server.ts",
    "test": "bun install --no-summary && bun --env-file=/dev/null run test-e2e.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "mqtt": "^5.10.0"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0"
  }
}
